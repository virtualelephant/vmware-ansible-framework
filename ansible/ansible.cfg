[defaults] inventory = inventories/lab/inventory.ini roles_path = roles host_key_checking = False stdout_callback = yaml timeout = 30 interpreter_python = auto retry_files_enabled = False  [ssh_connection] pipelining = True ssh_args = -o ControlMaster=auto -o ControlPersist=60s
